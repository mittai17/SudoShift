import React from 'react';
import { Link2 } from 'lucide-react';
import { createLinkNode } from '../shared/BaseLinkNode';
export default createLinkNode({ label: 'Event Link', accentColor: '#0ea5e9', icon: <Link2 className="w-4 h-4" /> });
