import React from 'react';
import { Table } from 'lucide-react';
import { createTableNode } from '../shared/BaseTableNode';
export default createTableNode({ label: 'Habit Tracking Table', accentColor: '#f97316', icon: <Table className="w-4 h-4" /> });
