import React from 'react';
import { Sigma } from 'lucide-react';
import { createFormulaNode } from '../shared/BaseFormulaNode';
export default createFormulaNode({ label: 'Note Formula', accentColor: '#6366f1', icon: <Sigma className="w-4 h-4" /> });
