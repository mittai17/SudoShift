import React from 'react';
import { Image } from 'lucide-react';
import { createImageNode } from '../shared/BaseImageNode';
export default createImageNode({ label: 'Note Image', accentColor: '#ff6d5a', icon: <Image className="w-4 h-4" /> });
