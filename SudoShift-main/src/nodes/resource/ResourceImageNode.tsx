import React from 'react';
import { Image } from 'lucide-react';
import { createImageNode } from '../shared/BaseImageNode';
export default createImageNode({ label: 'Resource Image', accentColor: '#8b5cf6', icon: <Image className="w-4 h-4" /> });
