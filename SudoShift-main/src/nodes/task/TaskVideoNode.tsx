import React from 'react';
import { Film } from 'lucide-react';
import { createVideoNode } from '../shared/BaseVideoNode';
export default createVideoNode({ label: 'Task Video', accentColor: '#22c55e', icon: <Film className="w-4 h-4" /> });
