import React from 'react';
import { Table } from 'lucide-react';
import { createTableNode } from '../shared/BaseTableNode';
export default createTableNode({ label: 'Event Agenda Table', accentColor: '#0ea5e9', icon: <Table className="w-4 h-4" /> });
