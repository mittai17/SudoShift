import React from 'react';
import { Film } from 'lucide-react';
import { createVideoNode } from '../shared/BaseVideoNode';
export default createVideoNode({ label: 'Event Meeting Video', accentColor: '#0ea5e9', icon: <Film className="w-4 h-4" /> });
