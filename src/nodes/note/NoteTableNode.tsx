import React from 'react';
import { Table } from 'lucide-react';
import { createTableNode } from '../shared/BaseTableNode';
export default createTableNode({ label: 'Note Table', accentColor: '#10b981', icon: <Table className="w-4 h-4" /> });
