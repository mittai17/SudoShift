import React from 'react';
import { Table } from 'lucide-react';
import { createTableNode } from '../shared/BaseTableNode';
export default createTableNode({ label: 'Project Table', accentColor: '#6366f1', icon: <Table className="w-4 h-4" /> });
