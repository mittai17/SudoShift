import React from 'react';
import { Film } from 'lucide-react';
import { createVideoNode } from '../shared/BaseVideoNode';
export default createVideoNode({ label: 'Resource Video', accentColor: '#8b5cf6', icon: <Film className="w-4 h-4" /> });
